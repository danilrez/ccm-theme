import { defaultColor } from '../colors/constants';

export const charts = {
	// Contrast color for text in charts
	'charts.foreground': defaultColor,
	// Color for lines in charts
	'charts.lines': defaultColor,
	// Color for red elements in charts
	'charts.red': defaultColor,
	// Color for blue elements in charts
	'charts.blue': defaultColor,
	// Color for yellow elements in charts
	'charts.yellow': defaultColor,
	// Color for orange elements in charts
	'charts.orange': defaultColor,
	// Color for green elements in charts
	'charts.green': defaultColor,
	// Color for purple elements in charts
	'charts.purple': defaultColor,
};
